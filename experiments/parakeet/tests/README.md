Safety and adapter tests are collected from `tests/test_parakeet_experiment.py`.
They do not download or load model weights unless the optional model smoke-test
environment variable is explicitly configured.
