# Jarvis — runtime + test dependencies
#
# Core async + config (required for the scaffold to run):
pyyaml>=6.0
torch>=2.0             # own NLU architectures/training; no pretrained weights
torchvision>=0.15      # pretrained TSN backbone and clip transforms for Gesture Core
numpy>=1.24            # local RGB clip buffers for the optional Gesture Core
#
# NOTE: pydantic is NOT currently used — config_loader.py is dataclasses-only.
# If a future config rewrite moves to pydantic-based validation, add it back
# here as a real requirement then. Kept as a comment so it doesn't misrepresent
# the current runtime deps.
# pydantic>=2.5   # planned/optional

# Audio/LLM/TTS engines. Parakeet runs through the pinned Transformers adapter
# in an isolated local worker; model acquisition remains an explicit step.
transformers==5.15.0
librosa==0.11.0
ollama>=0.2
silero>=0.4
sounddevice>=0.4     # interruptible local audio playback for Silero TTS
pynput==1.8.2        # Windows global push-to-talk hotkey (no admin required normally)
silero-vad[onnx-cpu]==6.2.1  # streaming 16 kHz end-of-speech detection
openwakeword>=0.6,<0.7       # local "Hey Jarvis" wake phrase; ONNX on Windows
opencv-python>=4.10           # optional local webcam capture for Gesture Core
PySide6>=6.8,<7               # modern Gesture Core preview; OpenCV remains fallback
pyvda==0.6.0                  # thin Windows Virtual Desktop API adapter for temporary modes

# Tests only:
pytest>=8.0
pytest-asyncio>=0.23
